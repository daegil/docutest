# LLM Wiki 프로젝트 초기화 프롬프트

> 사용법: `Initialize the project based on @INIT_PROMPT.md`

---

## 역할 정의

당신은 Docusaurus 기반 LLM Wiki 시스템 구축 전문가입니다.
이 프로젝트는 **두 가지 시스템을 하나의 저장소에서 운영**합니다:

- `docs/` — 프로젝트 문서 (계획, 실행, 의사결정, 회고). `DOCS.md` 규칙을 따름
- `wiki/` — LLM이 관리하는 지식 위키. `llm-wiki.md`의 패턴을 따름

Docusaurus는 이 둘의 **렌더링 유틸리티**로 활용됩니다.

## 현재 상황

- 이 디렉토리에 `DOCS.md`, `llm-wiki.md` 파일이 존재합니다
- `DOCS.md`에는 프로젝트 문서 작성 규칙이 정의되어 있습니다
- `llm-wiki.md`에는 LLM Wiki 패턴(3-Layer 아키텍처, Ingest/Query/Lint 워크플로우)이 설명되어 있습니다
- 두 파일을 **반드시 먼저 읽고** 규칙을 이해한 뒤 작업을 시작하세요

---

## 수행할 작업

### 1. 사전 확인

- `DOCS.md`를 읽고 프로젝트 문서 규칙을 파악합니다
- `llm-wiki.md`를 읽고 LLM Wiki 패턴(Raw Sources → Wiki → Schema)을 파악합니다

### 2. Docusaurus 프로젝트 초기화

- 현재 디렉토리에 새로운 Docusaurus 프로젝트를 생성합니다
- TypeScript 설정을 사용합니다 (`--typescript`)
- 임시 디렉토리에 생성 후 필요한 파일만 현재 디렉토리로 이동합니다
- scaffolding으로 생성된 `blog/`, `docs/`, `src/pages/` 디렉토리는 사용하지 않습니다

### 3. 디렉토리 구조 생성

```
프로젝트 루트/
├── raw/                          # Layer 1: 원본 자료 (immutable)
│   ├── articles/                 # 웹 아티클, 클리핑
│   ├── papers/                   # 논문, PDF
│   ├── notes/                    # 개인 메모, 회의록
│   └── assets/                   # 이미지 등 첨부파일
│
├── wiki/                         # Layer 2: LLM Wiki (LLM이 관리)
│   ├── index.md                  # 위키 전체 카탈로그
│   ├── log.md                    # 작업 로그 (append-only)
│   ├── entities/                 # 엔티티 페이지
│   ├── concepts/                 # 개념 페이지
│   ├── sources/                  # 소스 요약 페이지
│   └── synthesis/                # 종합 분석 페이지
│
├── docs/                         # 프로젝트 문서 (DOCS.md 규칙)
│   ├── intro.md                  # 프로젝트 문서 홈 (slug: /)
│   ├── 00-meta/
│   ├── 10-plan/
│   │   ├── drafts/
│   │   └── approved/
│   ├── 20-execution/
│   │   ├── logs/
│   │   ├── decisions/
│   │   └── meetings/
│   ├── 30-deliverables/
│   ├── 40-retros/
│   └── 90-reference/
│
├── src/css/custom.css            # Docusaurus 커스텀 CSS
├── static/                       # 정적 파일
├── CLAUDE.md                     # Layer 3: Schema (LLM 행동 규칙)
├── DOCS.md                       # 프로젝트 문서 운영 규약
├── llm-wiki.md                   # LLM Wiki 패턴 참조 문서
├── docusaurus.config.ts
├── sidebars.ts                   # docs/ 사이드바
├── sidebars-wiki.ts              # wiki/ 사이드바
├── package.json
└── tsconfig.json
```

### 4. docs/ 디렉토리 설정

- `DOCS.md`에 정의된 구조를 정확히 따릅니다
- 각 최상위 폴더 + 하위 폴더에 `_category_.json` 생성
- `docs/intro.md` 생성 — **반드시 `slug: /` 포함** (404 방지)
- 사이드바는 **autogenerated** 방식

### 5. wiki/ 디렉토리 설정

- `wiki/index.md` 생성 — 빈 카탈로그 구조 (Entities, Concepts, Sources, Synthesis 섹션). **`slug: /` 포함**
- `wiki/log.md` 생성 — init 기록 포함
- 각 하위 폴더(`entities/`, `concepts/`, `sources/`, `synthesis/`)에 `_category_.json` 생성
- 사이드바는 **autogenerated** 방식 (`sidebars-wiki.ts`)

### 6. Docusaurus 설정 (docusaurus.config.ts)

```typescript
// 핵심 설정 요약
const config: Config = {
  // docs/ → 루트 경로 (/)
  presets: [['classic', {
    docs: { sidebarPath: './sidebars.ts', routeBasePath: '/' },
    blog: false,  // 블로그 비활성화
  }]],

  // wiki/ → /wiki/ 경로 (멀티 docs 플러그인)
  plugins: [['@docusaurus/plugin-content-docs', {
    id: 'wiki',
    path: 'wiki',
    routeBasePath: 'wiki',
    sidebarPath: './sidebars-wiki.ts',
  }]],

  // Mermaid 다이어그램 지원
  markdown: { mermaid: true },
  themes: ['@docusaurus/theme-mermaid'],

  // navbar에 Docs + Wiki 링크
  // i18n: ko
};
```

### 7. CLAUDE.md 생성 (Schema)

CLAUDE.md는 LLM의 행동 규칙을 정의하는 **Schema 파일**입니다.
다음 섹션을 반드시 포함합니다:

#### 섹션 구성

1. **디렉토리별 규칙 구분** — `docs/` → DOCS.md, `wiki/` → Wiki 규칙, `raw/` → 읽기 전용
2. **docs/ 문서 작성 규칙** — DOCS.md 우선 참조
3. **wiki/ LLM Wiki 규칙**:
   - 3.1 구조 (entities, concepts, sources, synthesis)
   - 3.2 파일명 규칙 (엔티티명 kebab-case, 날짜 형식 미사용)
   - 3.3 프런트매터 규칙 (title, tags, author, contributors, created, updated, sources, status)
   - 3.4 링크 규칙 (표준 마크다운만, `[[wikilink]]` 금지 — Docusaurus/Obsidian 호환)
   - 3.5 페이지 작성 원칙
   - 3.6 파일 생성 후 UTF-8 검증 (필수 — `grep -rn '�' wiki/`)
4. **raw/ 원본 자료 규칙** — LLM은 절대 수정 금지
5. **Wiki 워크플로우** — Ingest, Query, Lint 상세 절차
6. **index.md 갱신 규칙** — 카테고리별 카탈로그, 알파벳순
7. **log.md 기록 규칙** — `## [YYYY-MM-DD] 작업유형 | 제목`, append-only

### 8. Mermaid 다이어그램 설정

- `@docusaurus/theme-mermaid` 패키지를 설치합니다
- `docusaurus.config.ts`에 `markdown.mermaid: true` + `themes` 설정

### 9. 불필요한 기능 제외

다음은 **절대 추가하지 않습니다**:

- 블로그 기능
- 불필요한 추가 플러그인 (Mermaid 테마는 예외)
- 커스텀 테마
- 복잡한 설정
- `src/pages/index.tsx` (routeBasePath '/' 사용 시 충돌)
- scaffolding으로 생성된 샘플 문서/블로그

### 10. 작업 완료 후 보고

설정이 끝나면 다음 내용을 출력합니다:

1. **생성된 구조 요약** — 디렉토리 트리, 주요 파일 목록
2. **3-Layer 아키텍처 설명** — raw/(소스) → wiki/(위키) → CLAUDE.md(스키마)
3. **다음 단계 안내**:
   - `npm start`로 로컬 서버 실행
   - `raw/`에 자료를 추가하고 "ingest 해줘"로 첫 위키 구축
   - `/wiki/`에서 위키 브라우징
   - 프로젝트 루트를 Obsidian vault로 열어 Graph View 활용

---

## 제약 조건

- `DOCS.md`의 규칙을 절대 위반하지 않습니다
- `llm-wiki.md`의 3-Layer 아키텍처를 따릅니다
- `docs/`와 `wiki/`는 서로 독립적이며 규칙이 간섭하지 않습니다
- 요청받지 않은 기능을 추가하지 않습니다
- 모든 설정은 명시적이고 이해하기 쉬워야 합니다

## 성공 기준

- `npm install && npm start` 명령어로 즉시 실행 가능
- `/` 경로에서 프로젝트 문서(docs/) 표시
- `/wiki/` 경로에서 LLM Wiki 표시
- navbar에서 Docs ↔ Wiki 전환 가능
- 사이드바에 모든 폴더가 올바르게 표시
- `CLAUDE.md`에 전체 Schema가 문서화
- Mermaid 다이어그램 렌더링 가능
- 프로젝트 루트를 Obsidian vault로 열 수 있음 (표준 마크다운 링크만 사용)
- 불필요한 파일이나 설정이 없음

---

## 알려진 주의사항 (Known Pitfalls)

### routeBasePath: '/' 사용 시 404 방지

`docs/intro.md`와 `wiki/index.md`에 **반드시 `slug: /`**를 추가해야 합니다.
없으면 Docusaurus가 루트 경로에 매핑할 문서를 찾지 못해 404가 발생합니다.
`src/pages/index.tsx`가 존재하면 Duplicate routes 충돌이 발생하므로 삭제합니다.

### MDX 중괄호 이스케이프

Docusaurus는 `.md` 파일도 MDX로 파싱합니다. `{}`가 JSX 표현식으로 해석되어 빌드 에러가 발생할 수 있습니다.
마크다운에서 중괄호를 사용해야 할 때는 **코드 블록(`` ` ``)으로 감싸야** 합니다.

### UTF-8 한글 깨짐

LLM이 파일을 생성할 때 한글의 UTF-8 바이트가 잘려 `�` 문자가 삽입될 수 있습니다.
**파일 생성 후 반드시 검증합니다**: `grep -rn '�' wiki/ docs/`
발견 시 즉시 수정합니다.

---

이제 작업을 시작하세요.
